type Mutation {
    registerUser(registerRequest: RegisterRequest): AuthResponse

    loginUser(loginRequest: LoginRequest): AuthResponse

    refreshUserToken: AuthResponse
}

type Query {
    authenticateUser(tokenRequest: TokenRequest): TokenResponse
}
